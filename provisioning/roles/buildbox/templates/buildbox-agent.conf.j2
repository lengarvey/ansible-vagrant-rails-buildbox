respawn
respawn limit 15 5

start on runlevel [2345]
stop on runlevel [06]

script
su - vagrant -c "/home/vagrant/.buildbox/buildbox-agent start --access-token {{ buildbox_access_token }}"
end script
